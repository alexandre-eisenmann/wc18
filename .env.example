# Copy to .env in this folder (Vite loads it automatically; never commit .env)
#
# Public key: safe in the browser. Test uses pk_test_*; switch to pk_live_* for production.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx
